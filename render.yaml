services:
  - type: web
    name: sukan-api
    env: python
    plan: free
    buildCommand: python -m pip install --upgrade pip && python -m pip install -r requirements.txt
    startCommand: python -m uvicorn apps.api.main:app --host 0.0.0.0 --port ${PORT:-10000}
    autoDeploy: true
